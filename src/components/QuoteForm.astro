---
import Button from './Button.astro';
import { SITE } from '../consts';

interface Props {
  serviceOptions?: string[];
  compact?: boolean;
}

const {
  compact = false,
  serviceOptions = [
    'Contract / office cleaning',
    'Industrial or deep cleaning',
    'Commercial kitchen deep clean',
    'Ducting and ventilation',
    'Window cleaning',
    'Data centre cleaning',
    'Other / multiple services',
  ],
} = Astro.props;

const turnstileSiteKey = import.meta.env.PUBLIC_TURNSTILE_SITE_KEY;
---

<form class="form bg-white rounded-card p-8 text-ink shadow-2xl" action="/api/quote" method="POST">
  <h3 class="text-xl font-bold mb-4">Request a Free Quote</h3>

  <div class="grid grid-cols-1 sm:grid-cols-2 gap-3">
    <div>
      <label for="firstName" class="block text-xs font-semibold text-grey mt-3 mb-1">First name</label>
      <input id="firstName" name="firstName" type="text" required placeholder="First name" class="form-input" />
    </div>
    <div>
      <label for="lastName" class="block text-xs font-semibold text-grey mt-3 mb-1">Last name</label>
      <input id="lastName" name="lastName" type="text" required placeholder="Last name" class="form-input" />
    </div>
  </div>

  <div class="grid grid-cols-1 sm:grid-cols-2 gap-3">
    <div>
      <label for="phone" class="block text-xs font-semibold text-grey mt-3 mb-1">Phone</label>
      <input id="phone" name="phone" type="tel" required placeholder="Phone number" class="form-input" />
    </div>
    <div>
      <label for="email" class="block text-xs font-semibold text-grey mt-3 mb-1">Email</label>
      <input id="email" name="email" type="email" required placeholder="Email address" class="form-input" />
    </div>
  </div>

  <label for="service" class="block text-xs font-semibold text-grey mt-3 mb-1">Which service are you interested in?</label>
  <select id="service" name="service" required class="form-input">
    {serviceOptions.map((opt) => <option value={opt}>{opt}</option>)}
  </select>

  <label for="message" class="block text-xs font-semibold text-grey mt-3 mb-1">Message</label>
  <textarea id="message" name="message" rows={compact ? 2 : 3} placeholder="Tell us about your site and requirements" class="form-input"></textarea>

  {
    turnstileSiteKey && (
      <div class="cf-turnstile mt-4" data-sitekey={turnstileSiteKey} data-theme="light" />
    )
  }

  <Button type="submit" variant="dark" class="w-full mt-4 justify-center !border-0">Request a Quote</Button>

  <small class="block mt-3 text-[11.5px] text-grey">
    By submitting this form you agree to our <a href="/privacy-policy/" class="underline hover:text-red">privacy policy</a> and consent to Ryak contacting you about your enquiry.
  </small>
</form>

{
  turnstileSiteKey && (
    <script src="https://challenges.cloudflare.com/turnstile/v0/api.js" async defer />
  )
}

<style>
  .form-input {
    @apply w-full border border-line rounded-md px-3 py-2.5 font-body text-sm bg-stone;
  }

  .form-input:focus {
    @apply outline-2 outline-red border-red bg-white;
  }
</style>
